# Copy this file to `.env` and adjust if needed.

# SQLite database location. Prisma resolves this relative to prisma/schema.prisma,
# so the default points at prisma/factory.db. The file is created by `prisma db push`.
DATABASE_URL="file:./factory.db"
